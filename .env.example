# Google Analytics 4 measurement ID for the "Pulse Landing" GA4 property
# (account "Pulse", account id 403209054, property id 548194152).
# Fill in once the web data stream is created for the production domain.
NEXT_PUBLIC_GA_MEASUREMENT_ID=G-9MVPGGPDF9

# HubSpot Private App token, server-side only (never expose as NEXT_PUBLIC_*).
# Used by /api/partners/lead to create/update contacts and deals.
HUBSPOT_PRIVATE_APP_TOKEN=
